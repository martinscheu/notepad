# Bind address on the host
# 0.0.0.0 = all interfaces (LAN + VPN)
BIND_ADDR=0.0.0.0

# Host port
HOST_PORT=8060

# Host path for notes data (bind-mounted into /data)
DATA_DIR_HOST=/opt/stickynotes/notes
